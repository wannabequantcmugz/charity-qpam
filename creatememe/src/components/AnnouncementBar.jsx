export default function AnnouncementBar() {
  return (
    <div className="bg-gradient-to-r from-accent via-purple-500 to-accent-light py-2 px-4 text-center">
      <p className="text-white text-xs font-mono font-medium tracking-wide">
        ? LAST CHANCE: 0.1 SOL CREATE COIN FEE (BACK TO 0.2 SOL IN 24H)
      </p>
    </div>
  )
}
